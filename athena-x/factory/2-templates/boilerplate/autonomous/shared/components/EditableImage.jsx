import React from 'react';
import EditableMedia from './EditableMedia';

/**
 * EditableImage (Legacy Alias for EditableMedia)
 * Autonomous track version.
 */
export default function EditableImage(props) {
  return <EditableMedia {...props} />;
}
